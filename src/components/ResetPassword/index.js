import React from 'react';
import { ResetPasswordForm } from './reset_password_form_base';

const ResetPasswordPage = () =>  (
    <div>
        <h1>Reset Password</h1>
        <ResetPasswordForm />
    </div>
);

export default ResetPasswordPage;
